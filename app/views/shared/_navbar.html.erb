<nav class="navbar navbar-inverse navbar-static-top" id="topnavbar">
  <div class="container-fluid">
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#collapsingnav">
        <span class="sr-only">Toggle Navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <div class="navbar-brand">
        <%= link_to image_tag('logo-navbar.png', class: 'logo'), root_path %>
      </div>
    </div>
    <div class="collapse navbar-collapse" id="collapsingnav">
      <ul class="nav navbar-nav navbar-right">
        <% if current_user %>
          <li class="navbar-text">
            Logged in as <%= current_user.name.capitalize %>
          </li>
          <li>
            <%= link_to logout_path, method: :delete do %>
              <span class="glyphicon glyphicon-log-out" aria-hidden="true"></span> Logout
            <% end %>
          </li>
        <% else %>
          <li>
            <%= link_to login_path do %>
              <span class="glyphicon glyphicon-log-in" aria-hidden="true"></span> Login with Spotify
            <% end %>
          </li>
        <% end %>
      </ul>
    </div>
  </div>
</nav>
